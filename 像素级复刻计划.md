# CustomInk 首页像素级复刻计划

**创建时间**: 2025-01-23  
**目标**: 精确复刻 https://www.customink.com/ 首页的所有视觉和交互细节

---

## 一、任务理解

### 核心目标
- 复刻 CustomInk 首页的完整视觉呈现
- 保持与原始设计像素级的精确度
- 确保响应式布局在所有设备上一致
- 实现所有交互效果和动画

### 限制条件
- **技术栈**: 纯 HTML + CSS + JavaScript（无框架）
- **浏览器兼容**: Chrome、Firefox、Safari、Edge 最新版
- **响应式断点**: 移动端（<768px）、平板（768px-1024px）、桌面（>1024px）
- **性能要求**: Lighthouse 评分 ≥ 90

---

## 二、复刻流程（6个阶段）

### 阶段1: 视觉资产收集与分析
**目标**: 获取所有设计元素和准确数据

**执行步骤**:
1. 使用浏览器 DevTools 截取每个区块的截图
2. 提取颜色值（十六进制）
3. 测量间距、字体大小、边框圆角
4. 记录字体族和字重
5. 下载所有图片素材（logo、产品图、品牌标识）

**工具**:
- Chrome DevTools (Elements, Network, Computed)
- Color Picker 扩展
- Pixel Perfect Chrome 扩展
- Figma/Penpot 进行布局测量

**预期输出**:
- `assets/` 目录包含所有图片
- `style-guide.md` 包含所有设计令牌

---

### 阶段2: HTML 结构搭建
**目标**: 准确还原语义化的 HTML 骨架

**审查要点**:
- `header#top-toolbar`: 顶部工具条（登录、购物车、电话）
- `header#primary-header`: 主导航（logo、主菜单、CTA按钮）
- `main`: 所有内容区块
  - `section#hero`: 英雄区块
  - `section#service-promise`: 服务承诺
  - `section#categories`: 产品分类
  - `section#brand-showcase`: 品牌展示
  - `section#testimonials`: 客户评价
  - `section#enterprise`: 企业服务
- `footer#footer`: 页脚

**验收标准**:
- ✅ 语义化标签正确使用
- ✅ 无障碍属性（ARIA）完整
- ✅ ID 和 class 命名清晰

---

### 阶段3: CSS 样式实现（重点阶段）
**目标**: 精确匹配所有视觉样式

**分块实现顺序**:
1. **基础重置和设计系统** (`:root` 变量)
   - 颜色、字体、间距、阴影、过渡
2. **全局布局**
   - `.container` 最大宽度和 padding
   - `.grid` 网格布局
3. **Top Toolbar**
   - 背景色、文字颜色、高度
4. **Primary Header**
   - Logo 尺寸、导航间距、按钮样式
5. **Hero Section**
   - 背景渐变、标题字号、CTA 按钮
6. **Service Promise**
   - 卡片布局、图标对齐
7. **Categories Grid**
   - 响应式网格、图片圆角、悬停效果
8. **Brand Showcase**
   - Logo 轮播、无限滚动效果
9. **Testimonials**
   - 星级评分、头像、引用样式
10. **Enterprise**
    - 对比布局、图片遮罩
11. **Footer**
    - 多列布局、链接样式

**验收标准**:
- ✅ Chrome DevTools 对比像素级差异 ≤ 2px
- ✅ 响应式断点平滑过渡
- ✅ 所有悬停和焦点状态完整

---

### 阶段4: JavaScript 交互实现
**目标**: 实现所有用户交互和动画

**功能列表**:
1. 移动端导航展开/收起
2. 滚动时 header 粘性定位
3. Hero 轮播图自动播放
4. Testimonials 自动切换
5. Brand logo 无限滚动
6. 懒加载图片
7. 平滑滚动
8. 表单验证（如有）

**验收标准**:
- ✅ 所有交互流畅
- ✅ 性能和内存无泄露
- ✅ 无障碍导航可用

---

### 阶段5: 响应式优化
**目标**: 确保所有设备上的视觉一致性

**测试清单**:
- [ ] iPhone SE (375px)
- [ ] iPhone 12/13/14 Pro (390px)
- [ ] iPhone 14 Pro Max (430px)
- [ ] iPad (768px)
- [ ] iPad Pro (1024px)
- [ ] MacBook Air (1280px)
- [ ] MacBook Pro (1440px)
- [ ] 4K 显示器 (1920px+)

**验收标准**:
- ✅ 所有断点布局正常
- ✅ 文字大小可读
- ✅ 图片比例不拉伸
- ✅ 触摸目标 ≥ 44×44px

---

### 阶段6: 性能优化与测试
**目标**: 提升加载速度和用户体验

**优化项**:
1. 图片压缩和格式优化（WebP + 后备）
2. CSS/JS 压缩和合并
3. 关键 CSS 内联
4. 非关键资源延迟加载
5. 添加缓存策略

**测试项**:
- [ ] Lighthouse 评分
- [ ] Core Web Vitals
- [ ] 跨浏览器测试
- [ ] 无障碍测试（a11y）

**验收标准**:
- ✅ Performance ≥ 90
- ✅ Accessibility ≥ 95
- ✅ Best Practices ≥ 95
- ✅ SEO ≥ 90

---

## 三、提示词规范模板

### 3.1 阶段提示词示例

#### 提示词模板 1: 视觉资产收集
```
任务：收集 CustomInk 首页的视觉资产

请按以下步骤执行：

1. 访问 https://www.customink.com/
2. 打开 Chrome DevTools
3. 对每个区块执行以下操作：
   - 截图保存为 区块名称.png
   - 提取计算后的 CSS 属性（color, font-size, padding, margin, border-radius 等）
   - 保存到 assets/ 目录
4. 在 style-guide.md 中记录：
   - 所有颜色值（十六进制）
   - 所有字体族和字号
   - 所有间距值
   - 所有圆角和阴影值

完成后，提供 style-guide.md 的文件路径。
```

#### 提示词模板 2: HTML 结构还原
```
任务：根据 CustomInk 首页截图，还原语义化的 HTML 结构

要求：
1. 严格按照原始 HTML 的嵌套结构
2. 使用语义化标签（header, nav, section, article, aside, footer）
3. 添加完整的 ARIA 属性（aria-label, aria-labelledby 等）
4. 为每个区块添加注释（如：<!-- 2025-01-23 10:30:00 Hero section -->）
5. 保持 HTML 格式整洁（缩进 2 空格）

请从 header#top-toolbar 开始，逐个区块还原到 footer#footer。
完成后展示完整的 HTML 代码。
```

#### 提示词模板 3: CSS 精确样式
```
任务：为 CustomInk 首页实现像素级精确的 CSS 样式

参考数据：style-guide.md 中的设计令牌

要求：
1. 使用 CSS 变量（:root）定义所有设计令牌
2. 实现移动端优先的响应式设计
3. 精确匹配原始设计的：
   - 颜色值（误差 ≤ 1px）
   - 字体大小和行高
   - 内外边距
   - 边框圆角和阴影
4. 添加所有交互状态（:hover, :focus, :active）
5. 使用 Flexbox/Grid 实现布局
6. 添加平滑过渡动画

从基础重置开始，逐个区块实现。每完成一个区块，请截图对比。

请开始实现 styles.css。
```

#### 提示词模板 4: JavaScript 交互实现
```
任务：为 CustomInk 首页添加所有 JavaScript 交互效果

要求：
1. 实现移动端导航展开/收起（使用 ARIA 状态）
2. 滚动时 header 添加 .sticky 类（防抖）
3. Hero 轮播图自动播放（5秒切换）
4. Testimonials 自动切换（8秒切换）
5. Brand logos 无限滚动
6. 图片懒加载（使用 Intersection Observer）
7. 所有动画使用 CSS transition，无需 JS

使用 ES6+ 语法，添加注释。完成后展示 app.js 完整代码。
```

#### 提示词模板 5: 响应式测试与修复
```
任务：在多个断点测试 CustomInk 首页的响应式表现

要求：
1. 在 Chrome DevTools 中测试以下视口：
   - 375px, 768px, 1024px, 1280px, 1920px
2. 检查每个区块的布局是否正常
3. 记录所有视觉问题
4. 修复断点问题，添加或调整 @media 查询

完成后，提供所有断点的截图对比。
```

#### 提示词模板 6: 性能优化
```
任务：优化 CustomInk 首页的加载性能

要求：
1. 使用 Lighthouse 运行性能审计（目标 ≥ 90 分）
2. 压缩所有图片到合适尺寸和格式（WebP）
3. 合并并压缩 CSS/JS
4. 添加 preload 关键资源
5. 延迟加载非关键图片
6. 添加 meta 标签（viewport, description）
7. 实现结构化数据（JSON-LD）

完成后，提供优化前后的 Lighthouse 报告对比。
```

---

### 3.2 Agent 协作提示词

#### 场景 1: 让 Agent A 专注于视觉收集
```
Agent A 的角色：视觉资产收集专家

你的任务：
1. 访问 https://www.customink.com/
2. 使用浏览器工具提取所有视觉数据
3. 生成 style-guide.md 和 assets/ 文件

完成后，将结果传递给 Agent B。
```

#### 场景 2: 让 Agent B 专注于代码实现
```
Agent B 的角色：前端开发专家

你的任务：
1. 读取 Agent A 提供的 style-guide.md
2. 根据设计令牌实现 HTML + CSS + JS
3. 确保像素级精确度

使用 Agent A 提供的资源，严格按照 style-guide.md 中的规格实现。
完成后，将代码交给 Agent C 测试。
```

#### 场景 3: 让 Agent C 专注于测试优化
```
Agent C 的角色：QA 和性能优化专家

你的任务：
1. 在多个设备上测试 Agent B 的代码
2. 使用 Lighthouse 进行性能审计
3. 修复所有问题并优化性能

完成后，提供最终的测试报告。
```

---

### 3.3 完整的端到端提示词
```
你的任务：像素级复刻 https://www.customink.com/ 首页

项目信息：
- 技术栈：纯 HTML/CSS/JS（无框架）
- 目标：视觉和交互 100% 还原
- 性能要求：Lighthouse ≥ 90

工作流程：
1. 访问 https://www.customink.com/ 并分析其结构
2. 提取所有设计数据（颜色、字体、间距、图片）
3. 创建以下文件结构：
   ```
   project/
   ├── index.html
   ├── styles.css
   ├── app.js
   ├── assets/
   │   ├── logo.svg
   │   ├── hero-products.jpg
   │   ├── brands/
   │   └── ...（所有图片）
   └── style-guide.md
   ```
4. 实现所有视觉样式和交互效果
5. 优化性能并测试响应式布局

执行原则：
- 每个修改添加注释和时间戳
- 使用语义化 HTML
- 添加完整的 ARIA 属性
- 移动端优先的响应式设计
- 所有交互平滑流畅

请开始执行，每完成一个阶段报告进度。
```

---

## 四、设计规范提取（待填充）

### 4.1 颜色系统
```
待从 CustomInk 首页提取：
- Primary Color: #_____
- Secondary Color: #_____
- Background: #_____
- Text Primary: #_____
- Text Secondary: #_____
- Border: #_____
- Accent: #_____
```

### 4.2 字体系统
```
待提取：
- Font Family Primary: _____
- Font Family Heading: _____
- Font Sizes: _____
- Line Heights: _____
- Font Weights: _____
```

### 4.3 间距系统
```
待提取：
- Base Unit: _____
- Spacing Scale: _____
- Breakpoints: _____
```

---

## 五、文件结构

```
project/
├── index.html          # 主 HTML
├── styles.css          # 所有样式
├── app.js              # 所有交互逻辑
├── assets/             # 所有资源
│   ├── logo.svg
│   ├── hero-products.jpg
│   ├── brands/
│   │   ├── nike.svg
│   │   ├── carhartt.svg
│   │   └── ...
│   └── avatars/
├── style-guide.md      # 设计令牌文档
└── 像素级复刻计划.md    # 本文件
```

---

## 六、验收清单

- [ ] 视觉还原度 100%（像素级对比）
- [ ] 响应式布局在所有设备正常
- [ ] 所有交互流畅无卡顿
- [ ] Lighthouse Performance ≥ 90
- [ ] Lighthouse Accessibility ≥ 95
- [ ] 跨浏览器兼容（Chrome、Firefox、Safari、Edge）
- [ ] HTML 语义化和无障碍属性完整
- [ ] 代码注释清晰，易于维护

---

## 七、交付物

1. ✅ 完整的 HTML/CSS/JS 代码
2. ✅ 所有图片和资源
3. ✅ 设计令牌文档（style-guide.md）
4. ✅ 测试报告（多设备截图 + Lighthouse）
5. ✅ 优化建议和后续迭代计划

---

**备注**: 本计划文档将根据实际情况持续更新。每完成一个阶段，请在该阶段前打 ✅。
