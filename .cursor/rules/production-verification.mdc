---
description: 新功能完成或部署到 GCP 后必须强提醒做线上验证，避免生产事故未被发现
alwaysApply: true
---

# 生产验证强提醒（全局习惯）

## 触发条件

在以下任一情况发生时，你**必须**在回复末尾输出「生产验证强提醒」区块，不得省略：

1. **完成新功能**：用户或你完成了新功能（合并、上线或标记为完成）
2. **部署到 GCP**：进行了任何部署到 GCP 的操作（Cloud Run、App Engine、GCE 等）

## 强提醒格式（必须使用）

在回复结尾单独成段，使用以下区块，保证用户一眼可见：

```
---
⚠️ **【必须】生产环境验证**

请立刻做一次线上验证，避免部署后生产已出问题却不知道：

- [ ] 打开生产环境 URL（网站/API），确认本次改动是否正常
- [ ] 按需验证关键流程：登录、主功能、支付/下单等
- [ ] 若有异常，立即回滚或修复，不要让生产长时间处于故障状态

---
```

## 规则说明

- 本条为**强制规则**，适用于任意项目；用户要求「强提醒」，因此每次触发条件满足时都必须输出上述区块。
- 若回复中已有类似提醒，也请保留或合并为上述统一格式，确保提醒足够醒目。
