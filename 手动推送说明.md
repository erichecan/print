# 手动推送代码到 GitHub

## 当前状态
- ✅ Git 已安装
- ✅ 代码已提交
- ✅ 远程仓库已连接：`https://github.com/erichecan/print.git`
- ⚠️ 需要 GitHub 认证

## 🎯 最简单的方法：使用个人访问令牌

### 步骤 1: 创建令牌

网页已自动打开：https://github.com/settings/tokens/new

**填写信息**：
1. Note: `git-push`
2. Expiration: `90 days`
3. 勾选权限：**✅ `repo`**（完整仓库访问）
4. 点击 "Generate token"

### 步骤 2: 复制令牌

⚠️ **重要**：令牌只显示一次！立即复制并保存。

### 步骤 3: 推送代码

在 PowerShell 中运行：

```powershell
git push -u origin main
```

**当提示输入凭证时**：
- Username: `erichecan`
- Password: **粘贴刚才复制的令牌**（不是您的 GitHub 密码！）

### 步骤 4: 创建 develop 分支

```powershell
git checkout -b develop
git push -u origin develop
```

---

## ✅ 完成！

推送成功后访问：
https://github.com/erichecan/print

---

## 📌 提示

如果以后需要推送代码，可以配置 Git Credential Manager 避免每次输入：

```powershell
# Windows 会自动记住凭证
git config --global credential.helper manager-core
```

---

**现在就去创建令牌，然后推送！** 🚀

