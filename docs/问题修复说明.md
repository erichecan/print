# 问题修复说明

**更新时间**: 2025-11-28 17:25:00

---

## 问题 1: Admin 数据库检查

### 检查方法

1. **本地数据库检查**:
```bash
cd backend
node scripts/check-products-and-admin.js
```

2. **GCP 生产环境检查**:
```bash
./scripts/check-gcp-admin-and-products.sh
```

3. **通过 API 检查**:
```bash
# 尝试登录验证
curl -X POST https://print-main-backend-hsbqzlnkxa-uc.a.run.app/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@suvernireplus.com","password":"admin123"}'

# 或者使用创建用户 API（如果用户不存在）
curl -X POST https://print-main-backend-hsbqzlnkxa-uc.a.run.app/api/admin-setup/create-user
```

### Admin 用户信息

- **邮箱**: `admin@suvernireplus.com`
- **密码**: `admin123`
- **角色**: `ADMIN`

### 如果 Admin 用户不存在

运行以下命令创建：
```bash
cd backend
node scripts/create-admin-user.js
```

或者在 GCP 上通过 API 创建：
```bash
curl -X POST https://print-main-backend-hsbqzlnkxa-uc.a.run.app/api/admin-setup/create-user
```

---

## 问题 2: 商品数据和 Clear All 按钮

### Clear All 按钮状态

✅ **已确认**: Clear All 按钮已被移除，因为系统已改为实时筛选（参考 Custom Ink 设计）。

**修改的测试**:
- `apps/web/tests/e2e/catalog.spec.ts` - 已更新测试，改为测试通过取消选择筛选条件来重置

### 商品数据检查

**检查脚本**:
```bash
cd backend
node scripts/check-products-and-admin.js
```

**如果数据库中没有商品**:

1. **检查迁移是否执行**:
```bash
cd backend
npx prisma migrate status
```

2. **运行 Seed 数据**:
```bash
cd backend
# 运行 Sequelize seeders
npx sequelize-cli db:seed:all

# 或者运行完整测试数据脚本
node scripts/seed-full-test-data.js
```

3. **检查数据库连接**:
```bash
# 检查 DATABASE_URL 是否正确
echo $DATABASE_URL
```

### 可能的问题

1. **迁移未执行**: 数据库表可能不存在
2. **Seed 未运行**: 表存在但数据为空
3. **连接错误**: DATABASE_URL 配置错误

---

## 问题 3: Stripe 密钥配置

### 安全提示

⚠️ **重要**: 请**不要**在聊天中直接发送 Stripe 密钥。应该通过以下方式配置：

### 配置方法

#### 方法 1: 使用环境变量文件（本地测试）

创建或编辑 `.env.test` 文件（在项目根目录）：
```bash
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here
```

#### 方法 2: 使用 GCP Secret Manager（推荐）

1. **创建 Secret**:
```bash
# 创建 Stripe Secret Key Secret
echo "sk_test_your_stripe_secret_key_here" | \
  gcloud secrets create stripe-secret-key \
  --project=moonlit-gamma-479502-r6 \
  --data-file=-

# 创建 Stripe Publishable Key Secret
echo "pk_test_your_stripe_publishable_key_here" | \
  gcloud secrets create stripe-publishable-key \
  --project=moonlit-gamma-479502-r6 \
  --data-file=-
```

2. **授予 Cloud Run 服务访问权限**:
```bash
# 获取服务账号
SERVICE_ACCOUNT=$(gcloud run services describe print-main-backend \
  --region=us-central1 \
  --project=moonlit-gamma-479502-r6 \
  --format="value(spec.template.spec.serviceAccountName)")

# 授予访问权限
gcloud secrets add-iam-policy-binding stripe-secret-key \
  --member="serviceAccount:${SERVICE_ACCOUNT}" \
  --role="roles/secretmanager.secretAccessor" \
  --project=moonlit-gamma-479502-r6

gcloud secrets add-iam-policy-binding stripe-publishable-key \
  --member="serviceAccount:${SERVICE_ACCOUNT}" \
  --role="roles/secretmanager.secretAccessor" \
  --project=moonlit-gamma-479502-r6
```

3. **在 Cloud Run 服务中配置**:
```bash
gcloud run services update print-main-backend \
  --update-secrets="STRIPE_SECRET_KEY=stripe-secret-key:latest,STRIPE_PUBLISHABLE_KEY=stripe-publishable-key:latest" \
  --region=us-central1 \
  --project=moonlit-gamma-479502-r6
```

#### 方法 3: 直接在 Cloud Run 环境变量中设置（临时测试）

```bash
gcloud run services update print-main-backend \
  --set-env-vars="STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here,STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here" \
  --region=us-central1 \
  --project=moonlit-gamma-479502-r6
```

### 配置测试环境变量

对于 Playwright 测试，创建或编辑 `apps/web/.env.test`:

```bash
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here
```

或者在运行测试时传递环境变量：

```bash
cd apps/web
STRIPE_SECRET_KEY=sk_test_your_key STRIPE_PUBLISHABLE_KEY=pk_test_your_key npm run test:gcp -- checkout-payment.spec.ts
```

### 支付测试重点

配置好 Stripe 密钥后，将重点测试：
1. ✅ 创建支付 Intent
2. ✅ 处理 Stripe 支付表单
3. ✅ 验证支付成功后的订单创建
4. ✅ 检查后台订单显示

---

## 下一步行动

1. ✅ 检查本地数据库状态（已完成）
2. ⏳ 检查 GCP 生产环境数据库状态
3. ⏳ 如果缺少数据，运行 seed
4. ⏳ 配置 Stripe 密钥
5. ⏳ 重新运行测试

---

**最后更新**: 2025-11-28 17:25:00

