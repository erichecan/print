# 工作日志 2026-06-04（下午）

## 本次完成内容

### 1. 可印刷区域标定与 Design Lab 适配系统（PRD-PRINTABLE-AREA-SYSTEM）

**Module A — 标定工具禁止 resize**
- `PrintableAreaCalibrator.tsx`：移除全部 8 个角点/边缘 resize handle，印刷框仅可整体拖动，无法调整大小
- 提示文字更新为"尺寸已锁定，仅可移动"

**Module C — Design Lab 新增元素自动适配印刷区域**
- 上传图片：等比缩放，较长边 = 印刷区域较长边的 80%，居中
- Artwork/SVG：等比缩放，较长边 = 印刷区域较长边的 60%，居中
- 文字：字号 = 印刷区域宽度 / 8，居中
- 修复缩放上限 bug：移除 `Math.min(..., 1)` 限制，小图也可正确放大

**Module D — 生产单显示物理印刷尺寸**
- `printable-area-templates.ts`：男款 T-shirt 写入 `physicalWidthIn: 16, physicalHeightIn: 18`（实测后调整为 18 inch），女款 15×20 inch
- Design Lab 导出 `printSpecs` 时写入 `widthIn`/`heightIn`
- 生产单读取时向后兼容 `pos.widthIn ?? pos.widthCm`

**尺寸调整**
- 男款 T-shirt 印刷框高度从 22 inch（960px）下调为 18 inch（785px），与实物匹配

### 2. 批量设定商品是否进入 Design Lab

- 后端新增 `PATCH /api/admin/online-products/batch/status`，支持 `enable_design_lab` / `disable_design_lab`
- 后端新增 `DELETE /api/admin/online-products/batch`（同时修正原 bulkDelete 路由错误）
- 管理后台商品列表下拉新增「启用 Design Lab」/「禁用 Design Lab」批量操作选项
- 控制字段为已有 `isCustomizable`，Design Lab 入口已在 `designController.js:88` 拦截不可定制商品

---

## 提交记录

| Hash | 说明 |
|------|------|
| `d9dd3aa7` | feat: printable area system — calibrator move-only, auto-scale, gang sheet dimensions |
| `3026ccfa` | fix: remove scale cap + tshirt height 22→18 inch |
| `2615375d` | feat: batch enable/disable Design Lab for products |

---

## 待测试 / 明天继续

- [ ] 标定工具调整各产品的 `x, y` 位置，与 Design Lab 底图对齐后保存，验证 Design Lab 同步生效
- [ ] 女款 T-shirt / Hoodie 等其他品类的印刷区域高度是否需要同步调整
- [ ] 生产单（gang sheet）显示物理尺寸，需要一个真实下单流程验证 `widthIn` 是否正确写入
- [ ] 第二个问题（用户提到"还有一个"）尚未展开讨论，明天确认
