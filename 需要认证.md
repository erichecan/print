# 🔐 需要 GitHub 认证

## ⚠️ 当前状态
Git 需要您的 GitHub 凭证才能推送代码。

## 🚀 快速解决方案

### **方法 1: 使用 GitHub CLI（最简单）**

GitHub CLI 已安装在您的系统上，可以自动处理认证！

```powershell
# 使用 gh 登录
gh auth login

# 然后推送
git push -u origin main
```

### **方法 2: 使用个人访问令牌**

如果您已经打开了 GitHub 令牌创建页面：

1. **填写信息**：
   - Note: `git-cli`
   - Expiration: 90 days
   - 勾选权限：**✅ `repo`**（完整仓库访问）

2. **生成令牌**：
   - 点击 "Generate token"
   - **复制令牌**（只显示一次！）

3. **在 PowerShell 推送**：
   - 用户名：`erichecan`
   - 密码：粘贴刚才复制的令牌

### **方法 3: 使用 GitHub Desktop**

如果您已经安装了 GitHub Desktop：

1. 打开 GitHub Desktop
2. File → Add local repository
3. 选择 `C:\Users\eric\Desktop\print`
4. Publish repository

---

## 🎯 推荐：使用 GitHub CLI

在 PowerShell 运行：

```powershell
gh auth login
```

按照提示：
1. 选择 GitHub.com
2. 选择 HTTPS
3. 选择 "Login with a web browser"
4. 按回车，会自动打开浏览器
5. 在浏览器中授权
6. 返回 PowerShell

然后推送：

```powershell
git push -u origin main
git checkout -b develop
git push -u origin develop
```

---

**选择任一方法完成认证！** 🔐

