# Agent 协作工作流程

**创建时间**: 2025-01-23  
**用途**: 指导如何让多个 Cursor AI Agent 协作完成像素级复刻任务

---

## 目录

1. [工作原理](#工作原理)
2. [单 Agent 模式](#单-agent-模式)
3. [多 Agent 协作模式](#多-agent-协作模式)
4. [最佳实践](#最佳实践)
5. [故障排除](#故障排除)

---

## 工作原理

### Cursor 的 Agent 特点

- **上下文感知**: 每个 Agent 都能读取工作区的所有文件
- **状态独立**: 不同 Agent 的对话上下文不共享
- **工具权限**: 所有 Agent 都能使用相同的工具（文件读写、搜索等）
- **任务专注**: 单个 Agent 专注一个任务时效果最好

### Agent 协作的核心思路

1. **分工**：按技能领域分工（视觉、代码、测试）
2. **交接**：通过文件系统传递信息
3. **验收**：每个阶段完成后验证
4. **迭代**：根据反馈调整

---

## 单 Agent 模式

**适用场景**: 
- 项目规模中等
- 需要一个统一的理解上下文
- 对一致性要求高

### 工作流程

```
用户 → Agent（一次性提示词）→ 完成所有阶段 → 提交结果
```

### 提示词示例

```
任务：像素级复刻 CustomInk 首页（完整流程）

请按顺序完成以下 6 个阶段，每完成一个阶段报告进度：

阶段 1: 收集视觉资产 → 生成 style-guide.md
阶段 2: 验证 HTML 结构 → 更新 index.html
阶段 3: 实现 CSS 样式 → 更新 styles.css
阶段 4: 添加 JS 交互 → 更新 app.js
阶段 5: 响应式测试 → 修复问题
阶段 6: 性能优化 → 提交最终版本

每个阶段完成后，在终端打印 ✅ 阶段 X 完成
```

### 优点
- ✅ 上下文连续，理解统一
- ✅ 不需要手动传递信息
- ✅ 迭代快速

### 缺点
- ❌ Agent 可能分心，偏离主题
- ❌ 个别阶段质量可能不佳
- ❌ 调试困难

---

## 多 Agent 协作模式

**适用场景**:
- 项目规模大
- 需要专业化技能
- 需要并行工作

### 工作流程（3-Agent 模式）

```
用户
  ↓
Agent A（视觉分析师）
  → 收集资产 → style-guide.md
  ↓
Agent B（前端开发）
  → 实现代码 → index.html, styles.css, app.js
  ↓
Agent C（QA 测试）
  → 测试优化 → 最终版本
  ↓
用户（验收）
```

---

## 详细执行步骤

### 第 1 步：启动 Agent A

在 Cursor 中新建对话，发送：

```
你的角色：前端视觉分析师

任务：收集 CustomInk 首页的所有视觉设计数据

要求：
1. 使用 web_search 工具搜索 CustomInk 首页的最新信息
2. 按照 资产收集清单.md 逐一提取设计数据
3. 生成 style-guide.md 文件，记录所有设计令牌
4. 完成后报告：✅ 视觉资产收集完成

请开始执行。
```

**等待 Agent A 完成**，检查生成的 `style-guide.md`。

---

### 第 2 步：启动 Agent B（第一部分：HTML）

在 Cursor 中新建对话（或继续使用同一个对话），发送：

```
你的角色：HTML 结构专家

前置工作：Agent A 已生成 style-guide.md

任务：验证并优化 index.html 的语义结构

要求：
1. 读取 style-guide.md（由 Agent A 提供）
2. 读取当前 index.html
3. 对比 https://www.customink.com/，检查结构是否完整
4. 使用语义化 HTML5 标签
5. 添加完整的 ARIA 属性
6. 为每段代码添加注释和时间戳

完成后报告：✅ HTML 结构验证完成
```

---

### 第 3 步：Agent B（第二部分：CSS）

在同一对话中，发送：

```
你的角色：CSS 样式专家

前置工作：
- style-guide.md（由 Agent A 提供）
- index.html（已更新）

任务：实现像素级精确的 CSS 样式

要求：
1. 读取 style-guide.md 中的所有设计令牌
2. 使用 CSS 变量定义所有设计值
3. 移动端优先的响应式设计
4. 精确匹配颜色、字体、间距（误差 ≤ 2px）
5. 添加所有交互状态

实现顺序：全局变量 → 布局 → 各区块样式

完成后报告：✅ CSS 样式实现完成
```

---

### 第 4 步：Agent B（第三部分：JavaScript）

在同一对话中，发送：

```
你的角色：JavaScript 交互专家

前置工作：HTML/CSS 已实现

任务：添加所有 JavaScript 交互效果

要求：
1. 实现移动端导航展开/收起
2. 实现滚动时 header 粘性定位
3. 实现 Hero 轮播图自动播放
4. 实现 Testimonials 自动切换
5. 实现图片懒加载
6. 使用 ES6+ 语法，添加注释

完成后报告：✅ JavaScript 交互实现完成
```

---

### 第 5 步：启动 Agent C

在 Cursor 中新建对话，发送：

```
你的角色：QA 测试和性能优化专家

前置工作：Agent B 已实现所有代码

任务：
1. 测试响应式布局（375px, 768px, 1024px, 1920px）
2. 使用 Lighthouse 进行性能审计
3. 优化图片和资源
4. 修复所有问题

要求：
1. 在多个断点截图对比
2. 确保 Lighthouse Performance ≥ 90
3. 确保响应式布局正常
4. 记录所有修复的问题

完成后报告：✅ 测试和优化完成
```

---

### 第 6 步：验收

检查最终结果：

- [ ] 打开 index.html 在浏览器预览
- [ ] 对比原始网站 https://www.customink.com/
- [ ] 检查多个断点的响应式布局
- [ ] 运行 Lighthouse 审计
- [ ] 检查所有交互是否流畅

---

## 三种协作策略对比

### 策略 1：串行协作（推荐）

```
Agent A → Agent B → Agent C → 用户验收
```

**优点**: 
- 信息传递清晰
- 每个阶段质量可控
- 容易调试问题

**缺点**: 
- 总时间较长

**适用**: 首次复刻、质量优先

---

### 策略 2：并行协作

```
用户 → Agent A（视觉）┐
                    ├→ Agent D（整合）→ 用户
用户 → Agent B（HTML）│
                    └→ Agent C（CSS）
```

**优点**: 
- 速度快

**缺点**: 
- 需要整合逻辑
- 可能出现冲突

**适用**: 项目规模大、有经验

---

### 策略 3：混合协作

```
Agent A（视觉）→ Agent B（HTML/CSS）┐
                                     ├→ Agent C（测试）
Agent D（部分区块）→ Agent E（部分区块）┘
```

**优点**: 
- 灵活

**缺点**: 
- 需要协调

**适用**: 大型项目、多模块

---

## 交接文档模板

### Agent A 传给 Agent B

```
交接报告

Agent A 已完成：视觉资产收集
时间：2025-01-23 10:30:00

交付文件：
- style-guide.md（设计令牌）
- assets/（所有图片）

关键设计值：
- Primary Color: #FF6600
- Font Family: System UI
- Container Max Width: 1200px
- Base Spacing: 4px

状态：✅ 完成

Agent B，请基于这些资源开始实现代码。
```

---

### Agent B 传给 Agent C

```
交接报告

Agent B 已完成：HTML/CSS/JS 实现
时间：2025-01-23 11:45:00

交付文件：
- index.html（语义化结构）
- styles.css（像素级样式）
- app.js（所有交互）

代码质量：
- ✅ HTML 语义化完整
- ✅ CSS 变量使用正确
- ✅ JS 注释清晰
- ⚠️  需要测试响应式布局

已知问题：
- 平板断点可能需要微调

Agent C，请开始测试和优化。
```

---

## 故障排除

### 问题 1: Agent 无法读取文件

**原因**: 文件路径错误

**解决**:
```
请使用 read_file 工具读取文件。
文件路径：./
先列出目录看看有哪些文件。
```

---

### 问题 2: Agent 忘记上下文

**原因**: 对话丢失上下文

**解决**:
```
提醒 Agent：
1. 你之前正在做什么（提供上下文）
2. 当前应该做什么（明确任务）
3. 如何验证完成（验收标准）
```

---

### 问题 3: Agent 偏离主题

**原因**: 提示词不明确

**解决**:
```
立即纠正：
停止当前操作，按照以下指示重新开始：
1. [明确任务]
2. [具体步骤]
3. [验收标准]
```

---

### 问题 4: Agent 之间的工作冲突

**原因**: 多 Agent 同时修改同一文件

**解决**:
```
立即停止所有 Agent，然后：
1. 检查 git 状态
2. 查看冲突文件
3. 手动合并或选择保留哪个版本
4. 重新分配任务
```

---

## 最佳实践

### 1. 明确的角色定义

```
❌ 差：帮我做一个网站
✅ 好：你是前端视觉分析师，任务是收集设计数据
```

---

### 2. 清晰的任务分解

```
❌ 差：完成所有工作
✅ 好：阶段 1：收集颜色、字体、间距 → 生成 style-guide.md
```

---

### 3. 明确的验收标准

```
❌ 差：做完后告诉我
✅ 好：完成后在终端打印 ✅ 阶段 1 完成，并展示 style-guide.md 的内容
```

---

### 4. 及时反馈

```
❌ 差：等所有阶段完成后再说
✅ 好：每完成一个阶段立即检查并反馈
```

---

### 5. 保存对话记录

```
✅ 为每个 Agent 保存对话记录
✅ 记录所有重要决策
✅ 跟踪进度和问题
```

---

## 快速开始

### 一步到位：单 Agent 模式

复制整个提示词到 Cursor：

```
任务：像素级复刻 CustomInk 首页

参考文档：像素级复刻计划.md

请按 6 个阶段顺序执行：
1. 收集视觉资产
2. 验证 HTML 结构
3. 实现 CSS 样式
4. 添加 JS 交互
5. 测试响应式
6. 优化性能

每完成一个阶段报告进度。
```

---

### 两步法：快速验证

**步骤 1**: 让 Agent 收集和分析

```
任务：分析 CustomInk 首页并生成 style-guide.md

请提取所有设计数据并创建设计指南文档。
```

**步骤 2**: 让 Agent 实现代码

```
任务：根据 style-guide.md 实现完整的 HTML/CSS/JS

要求像素级精确度，完全响应式。
```

---

## 总结

**单 Agent 模式**适合：
- 小型项目
- 需要连贯理解
- 时间紧迫

**多 Agent 协作**适合：
- 大型项目
- 专业化分工
- 质量优先

选择哪种模式，取决于你的项目规模和质量要求。

---

**下一步**: 开始使用 [Agent协作提示词模板.md](./Agent协作提示词模板.md) 中的提示词，让 Agent 帮你复刻 CustomInk 首页！

---

**更新日志**:
- 2025-01-23: 初版创建
