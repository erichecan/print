# Netlify configuration scoped to apps/web
# [2025-11-14 06:38:00] 与根 netlify.toml 保持一致，启用 SSR 模式

[build]
  command = "npm install && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"
  NEXT_TELEMETRY_DISABLED = "1"

